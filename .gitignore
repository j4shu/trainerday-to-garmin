# Garmin OAuth tokens (default store is ~/.garminconnect, outside the repo;
# ignore any local copies)
.garminconnect/
garmin_tokens.json

# Secrets (intervals.icu API key, loaded via python-dotenv)
.env

# Python
__pycache__/
*.py[cod]
.venv/

# Claude
.claude/